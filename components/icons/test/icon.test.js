import '../icon.js';
import { runConstructor } from '@brightspace-ui/testing';

describe('d2l-icon', () => {

	describe('constructor', () => {

		it('should construct', () => {
			runConstructor('d2l-icon');
		});

	});

});
