import '../input-group.js';
import { runConstructor } from '@brightspace-ui/testing';

describe('d2l-input-group', () => {

	describe('constructor', () => {

		it('should construct', () => {
			runConstructor('d2l-input-group');
		});

	});

});
