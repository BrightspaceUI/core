import '../page-header-custom.js';
import { runConstructor } from '@brightspace-ui/testing';

describe('d2l-page-header-custom', () => {

	it('should construct', () => {
		runConstructor('d2l-page-header-custom');
	});

});
