import '../page-header-link-back.js';
import { runConstructor } from '@brightspace-ui/testing';

describe('d2l-page-header-link-back', () => {

	describe('constructor', () => {
		it('should construct', () => {
			runConstructor('d2l-page-header-link-back');
		});
	});

});
