import '../card.js';
import { runConstructor } from '../../../tools/constructor-test-helper.js';

describe('d2l-card', () => {

	it('should construct', () => {
		runConstructor('d2l-card');
	});

});
