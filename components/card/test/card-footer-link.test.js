import '../card-footer-link.js';
import { runConstructor } from '@brightspace-ui/testing';

describe('d2l-card-footer-link', () => {

	it('should construct', () => {
		runConstructor('d2l-card-footer-link');
	});

});
